import { Box, Typography, Grid, Stack, Container } from '@mui/material';

export default function Input() {
    return (
        <Box
            sx={{
                bgcolor: '#e6e0c5',
                p: 1,
                borderRadius: 2
            }}
        >
            Input
        </Box>
    );
}
